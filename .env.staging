APP_ENV=staging
EXPO_PUBLIC_API_URL=https://staging.api.example.com
EXPO_PUBLIC_REFRESH_URL=https://staging.api.example.com/auth/refresh

# Silence dotenv's random tip line — it can break expo-doctor's JSON parsing (upstream issue)
DOTENV_CONFIG_QUIET=true
